/*jshint unused:false */
var gdate, date, a, b;

date.compare(a, b) > -1;
date.compare(a, b) > 1;

var prevMonth, value, currMonth;
var validTValue = gdate.compare(prevMonth, value) === 1 ? prevMonth : currMonth;